apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-demo
spec:
  project: default

  source:
    repoURL: https://github.com/TEU-USUARIO/gitops-project
    targetRevision: HEAD
    path: app

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
